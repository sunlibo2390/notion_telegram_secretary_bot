[general]
timezone_offset_hours = 8

[paths]
data_dir = "D:/Projects/codex_test/notion_secretary/databases"
database_ids_path = "database_ids.json"

[notion]
api_key = "secret_xxx"
sync_interval = 1800
force_update = false
api_version = "2022-06-28"

[telegram]
token = "8096:ABCDEF"
poll_timeout = 25
admin_ids = [6604771431]

[llm]
provider = "openai"
base_url = "https://api.openai.com/v1"
model = "gpt-4o-mini"
temperature = 0.3
api_key = "sk-..."

[tracker]
interval_seconds = 1500
follow_up_seconds = 600

[wecom]
webhook_url = ""  # 可选：填写企业微信机器人 webhook，用于同步 Agent 回复

[proactivity]
state_check_seconds = 300              # 状态巡检间隔
state_stale_seconds = 3600            # 超过 1 小时未更新状态就主动询问
state_prompt_cooldown_seconds = 600   # 状态询问的冷却时间
question_follow_up_seconds = 600      # Agent 提问后未回复的追问间隔（默认 10 分钟）
state_unknown_retry_seconds = 120     # 状态为 unknown 或过期时的追问间隔
